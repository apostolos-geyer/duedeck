services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: duedeck
      POSTGRES_PASSWORD: duedeck
      POSTGRES_DB: duedeck
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
